# Next.js inlines NEXT_PUBLIC_* at build time. Include /api/v1 for JSON REST.
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# Server-side Next rewrite target; production browsers remain same-origin.
API_ORIGIN=http://api:4000
NEXT_PUBLIC_OIDC_CLIENT_ID=web
