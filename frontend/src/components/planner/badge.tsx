/**
 * Compatibility shim for Release 2 consumers.
 * Re-exports the shared Badge primitive from components/ui/badge.
 */
export { Badge, type BadgeProps, type BadgeTone } from "@/components/ui/badge";
