"use client";
import Link from "next/link";
import { useRouter, useSearchParams } from "next/navigation";
import { useQuery } from "@tanstack/react-query";
import { useState } from "react";
import { sdk } from "@/lib/sdk";
import { useAccounts, useCategories, useRefreshFinancialViews, unwrap } from "./queries";
import { action, secondary, control, Field, Panel, PageTitle, Loading, ErrorNotice, Empty, money, date } from "./ui";

export function Transactions() {
  const params = useSearchParams(); const router = useRouter();
  const direction = params.get("direction") as "DEBIT" | "CREDIT" | null;
  const status = params.get("status") as "verified" | "needs_review" | "pending" | null;
  const accountId = params.get("accountId") || undefined;
  const categoryId = params.get("categoryId") || undefined;
  const startDate = params.get("startDate") || undefined;
  const endDate = params.get("endDate") || undefined;
  const query = useQuery({ queryKey: ["transactions", params.toString()], queryFn: async () => unwrap(await sdk.GET("/api/v1/transactions", { params: { query: { limit: 25, cursor: params.get("cursor") || undefined, direction: direction || undefined, status: status || undefined, accountId, categoryId, startDate, endDate } } })) });
  const accounts = useAccounts();
  const categories = useCategories();
  function filter(key: string, value: string) { const next = new URLSearchParams(params); next.delete("cursor"); if (value) next.set(key, value); else next.delete(key); router.push(`/dashboard/transactions?${next}`); }
  return <><PageTitle title="Transactions" description="Recorded activity is separate from the monthly amounts in your plan."><Link className={action} href="/dashboard/transactions/new">Add transaction</Link></PageTitle><Panel title="Recorded activity"><div className="mb-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><label>Direction<select className={`${control} mt-2`} value={direction ?? ""} onChange={e => filter("direction", e.target.value)}><option value="">All directions</option><option value="DEBIT">Money out</option><option value="CREDIT">Money in</option></select></label><label>Status<select className={`${control} mt-2`} value={status ?? ""} onChange={e => filter("status", e.target.value)}><option value="">All statuses</option><option value="needs_review">Needs review</option><option value="verified">Verified</option><option value="pending">Pending</option></select></label><label>Account<select className={`${control} mt-2`} value={accountId ?? ""} onChange={e => filter("accountId", e.target.value)}><option value="">All accounts</option>{accounts.data?.map(a => <option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label>Category<select className={`${control} mt-2`} value={categoryId ?? ""} onChange={e => filter("categoryId", e.target.value)}><option value="">All categories</option>{categories.data?.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select></label><label>From date<input type="date" className={`${control} mt-2`} value={startDate ?? ""} onChange={e => filter("startDate", e.target.value)} /></label><label>To date<input type="date" className={`${control} mt-2`} value={endDate ?? ""} onChange={e => filter("endDate", e.target.value)} /></label></div><ErrorNotice error={query.error} retry={() => void query.refetch()} />{query.isPending && <Loading />}{query.data?.data.length === 0 && <Empty>No transactions match these filters. Add a manual transaction or change the filters.</Empty>}<ul className="divide-y divide-border">{query.data?.data.map(t => <li key={t.id}><Link href={`/dashboard/transactions/${t.id}`} className="flex min-h-20 flex-wrap items-center justify-between gap-3 rounded-lg py-4 focus-visible:outline-2 focus-visible:outline-primary"><div><p className="font-semibold">{t.merchantName || t.description || "Manual transaction"}</p><p className="text-sm text-muted-foreground">{date(t.occurredAt)} · {t.status.replaceAll("_", " ")}</p></div><div className="text-right"><p className="tabular-nums font-semibold">{money(t.amount, t.currency)}</p><p className="text-sm">{t.direction === "DEBIT" ? "Money out" : "Money in"}</p></div></Link></li>)}</ul><div className="mt-5 flex gap-3">{params.has("cursor") && <button className={secondary} onClick={() => filter("cursor", "")}>First page</button>}{query.data?.nextCursor && <button className={secondary} onClick={() => { const next = new URLSearchParams(params); next.set("cursor", query.data!.nextCursor!); router.push(`/dashboard/transactions?${next}`); }}>Next page</button>}</div></Panel></>;
}

export function TransactionEditor({ id }: { id?: string }) {
  const accounts = useAccounts(); const categories = useCategories(); const refresh = useRefreshFinancialViews(); const router = useRouter(); const [error, setError] = useState<unknown>(); const [pending, setPending] = useState(false); const [confirmDelete, setConfirmDelete] = useState(false);
  const query = useQuery({ queryKey: ["transactions", id], enabled: !!id, queryFn: async () => unwrap(await sdk.GET("/api/v1/transactions/{id}", { params: { path: { id: id! } } })).data });
  async function submit(form: FormData) {
    setPending(true); setError(null);
    const get = (key: string) => String(form.get(key) ?? "");
    try {
      const common = { merchantName: get("merchantName"), description: get("description"), accountId: get("accountId") || undefined, categoryId: get("categoryId") || undefined };
      if (id) unwrap(await sdk.PATCH("/api/v1/transactions/{id}", { params: { path: { id } }, body: { ...common, accountId: get("accountId") || null, categoryId: get("categoryId") || null, status: get("status") as "verified" | "needs_review" | "pending" } }));
      else unwrap(await sdk.POST("/api/v1/transactions", { body: { ...common, amount: get("amount"), direction: get("direction") as "DEBIT" | "CREDIT", currency: "INR", occurredAt: new Date(get("occurredAt")).toISOString() } }));
      await refresh(); router.push("/dashboard/transactions");
    } catch (e) { setError(e); } finally { setPending(false); }
  }
  async function remove() {
    if (!id) return;
    setPending(true); setError(null);
    try { unwrap(await sdk.DELETE("/api/v1/transactions/{id}", { params: { path: { id } } })); await refresh(); router.push("/dashboard/transactions"); }
    catch (e) { setError(e); setPending(false); }
  }
  if (id && query.isPending) return <Loading />;
  return <><PageTitle title={id ? "Review transaction" : "Add transaction"} description={id ? "Review the merchant, account, category, description and status." : "Record an observation without changing your monthly plan."} /><div className="max-w-2xl space-y-6"><Panel title={id ? "Transaction details" : "Manual entry"}><ErrorNotice error={query.error} retry={() => void query.refetch()} />{(!id || query.data) && <form action={submit} className="space-y-4">{id ? <p className="text-xl tabular-nums">{money(query.data?.amount, query.data?.currency)} · {date(query.data?.occurredAt)} · {query.data?.direction === "DEBIT" ? "Money out" : "Money in"}</p> : <><Field label="Amount (INR)" name="amount" required inputMode="decimal" pattern="[0-9]+(\.[0-9]{1,2})?" /><label className="block">Direction<select name="direction" className={control}><option value="DEBIT">Money out</option><option value="CREDIT">Money in</option></select></label><Field label="Date and time" name="occurredAt" type="datetime-local" required /></>}<Field label="Merchant" name="merchantName" maxLength={255} defaultValue={query.data?.merchantName ?? ""} /><Field label="Description" name="description" maxLength={500} defaultValue={query.data?.description ?? ""} /><label className="block">Account<select name="accountId" className={control} defaultValue={query.data?.accountId ?? ""}><option value="">Unassigned</option>{accounts.data?.map(a => <option key={a.id} value={a.id}>{a.name}</option>)}</select></label><label className="block">Category<select name="categoryId" className={control} defaultValue={query.data?.categoryId ?? ""}><option value="">Uncategorized</option>{categories.data?.map(c => <option key={c.id} value={c.id}>{c.name}</option>)}</select></label>{id && <label className="block">Review status<select name="status" className={control} defaultValue={query.data?.status}><option value="verified">Verified</option><option value="needs_review">Needs review</option><option value="pending">Pending</option></select></label>}<ErrorNotice error={error} /><div className="flex flex-wrap gap-3"><button className={action} disabled={pending}>{pending ? "Saving…" : "Save transaction"}</button><Link className={secondary} href="/dashboard/transactions">Cancel</Link></div></form>}</Panel>{id && <Panel title="Delete transaction">{confirmDelete ? <div className="space-y-3"><p>Delete this recorded transaction? This cannot be undone.</p><button id="confirm-transaction-delete" className={secondary} disabled={pending} onClick={() => void remove()}>{pending ? "Deleting…" : "Confirm delete"}</button><button className={secondary} disabled={pending} onClick={() => { setConfirmDelete(false); requestAnimationFrame(() => document.getElementById("delete-transaction")?.focus()); }}>Keep transaction</button></div> : <button id="delete-transaction" className={secondary} onClick={() => { setConfirmDelete(true); requestAnimationFrame(() => document.getElementById("confirm-transaction-delete")?.focus()); }}>Delete transaction</button>}</Panel>}</div></>;
}
