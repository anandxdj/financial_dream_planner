# F12 - Plan History & Drift

## Routes
`/dashboard/plan/history`, `/dashboard/plan/review/[id]`, version detail.

## Drift review
Show what changed -> impact -> proposed adjustment.

## Actions
Accept new baseline / Keep current plan / Review transactions.

## History
Meaningful version differences. Restore creates a new version; never deletes later history.
